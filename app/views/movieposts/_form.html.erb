<%= form_for @moviepost, :html => {:multipart => true } do |f| %>
  <% if @moviepost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@moviepost.errors.count, "error") %> prohibited this moviepost from being saved:</h2>

      <ul>
      <% @moviepost.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :img %><br>
    <%= f.file_field :movieimage %>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
